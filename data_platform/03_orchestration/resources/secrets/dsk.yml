resources:
  secret_scopes:
    dsk:
      name: ${bundle.target}-dsk-kv-secret-scope
      backend_type: AZURE_KEYVAULT
      keyvault_metadata:
        resource_id: ${var.keyvault_resource_id}
        dns_name: ${var.keyvault_dns_name}
      permissions:
      - user_name: nagarajarjun@jmangroup.com
        level: MANAGE
